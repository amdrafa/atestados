import { FormGrid } from "./FormGrid";
import { FormLabel } from "./FormLabel";
import { FormRoot } from "./FormRoot";

export const Form = {
    Root: FormRoot,
    Grid: FormGrid,
    Label: FormLabel
}